<svg viewBox="0 0 36 48" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Pin body with ð shape -->
  <!-- Outer circle (body of ð / head of pin) -->
  <circle cx="18" cy="18" r="14" fill="#f59e0b"/>
  <!-- Inner counter (hole of the ð) -->
  <circle cx="18" cy="18" r="6.5" fill="white"/>
  <!-- Pin tail (descender) -->
  <path d="M12 28C12 28 18 46 18 46C18 46 24 28 24 28" fill="#f59e0b"/>
  <!-- Smooth the join between circle and tail -->
  <rect x="12" y="26" width="12" height="4" fill="#f59e0b"/>
  <!-- Eth cross-stroke (the defining ð mark) -->
  <line x1="6" y1="12" x2="30" y2="12" stroke="white" stroke-width="3" stroke-linecap="round"/>
  <!-- Ascender stroke curving up-right -->
  <path d="M24 18C26 14 28 8 26 4" stroke="#f59e0b" stroke-width="3.5" stroke-linecap="round" fill="none"/>
</svg>
